[SSH]
name,M&S_cluster,Name of cluster in Pirana
login,ssh user@server, (SSH-mode only) Command used to login to remote server using OpenSSH
remote_folder,/home/user, (SSH-mode only) mount location of cluster data on server
local_folder,Z:, (SSH-mode only) mount location of cluster data on local comp
parameters,, Additional parameters for SSH
execute_before,, Command to execute before tunneled command, e.g. '. /etc/profile'
